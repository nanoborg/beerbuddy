<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="/style.css" />
        <title>Show all beer</title>
    </head>
    <body>
        <h1>The pub name is</h1>
        <p>
            <%= pubInfo[0].pubname %>
        </p>
        
        <p>
            <% for (var i = 0; i < beerTypes.length; i++) { %>
                <%=beerTypes[i].beertype%> || <%=beerTypes[i].beerbrand%>
                
                <p><%=Number(beerTypes[i].avg).toFixed(2)%></p>
            <% }%>
        
        </p>    

        <a href="/beers/new/<%= pubInfo[0].id %>">Rate Beers</a>
    </body>
</html>
